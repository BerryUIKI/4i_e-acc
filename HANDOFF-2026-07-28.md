# Handoff — 2026-07-28

> 项目：《四倍做多认知，长期做多人生》  
> 分支：`article/2026-quadruple-long-life` → PR #7（待 squash-merge 到 main）

---

## 1. 项目概览

一本中文投资入门书，30 章正文 + 后记 + 12 附录，约 95,000 字草稿。

```
articles/2026-quadruple-long-life/
├── Front-Matter/     # 开篇声明、导读、序言
├── Main-Text/        # A:财富观 B:复利与风险 C:资产类别 D:指数与ETF E:配置 F:执行 G:心法 H:后记
├── Appendices/       # A-L 附录
├── analysis/         # Python 数据分析脚本 + 图表（可复现）
├── illustrations/    # 插图规格说明（ALL-SPECS.md）
├── assets/           # IP形象 + 插图成品
├── HANDOFF-TO-GPT.md # 插图子Agent交接
├── todo.md           # 任务清单
├── notes.md          # 笔记
└── references.md     # 引用
```

---

## 2. 今日完成的工作

### 2.1 数据缺口填充
12 处 `[数据待补充]` 全部替换为真实数据 + Python 图表（`analysis/src/*.py`，数据来源标注，代码可复现）。

### 2.2 章节内容补充
- Ch23 家庭配置案例六~十一（自由职业者、房产集中、高管股权、100/300/1000 万档）
- Ch30 行为经济学引用 6 条
- 货币基金收益率、国债收益率曲线图表

### 2.3 插图规格
18 份插图 spec（`illustrations/ALL-SPECS.md`），中文 prompt，嵌入文字要求，无"花有财"角色名（用参考图 + `依据参考图形象生成：` 前缀）。

### 2.4 Git LFS
`.gitattributes` 配置：`*.png *.jpg *.jpeg *.csv *.pdf` → Git LFS。LFS 文件已迁移推送。

### 2.5 子Agent工作流
`AGENTS.md` 记录了子Agent使用流程：主Agent写 handoff MD → 用户开子Agent → 贴Prompt（代码块格式）。

---

## 3. CI/CD — PDF 自动编译（重要）

### 3.1 架构
```
push to article/* or main
  → GitHub Actions (.github/workflows/build-book-pdf.yml)
    → apt install texlive + pandoc + noto-cjk
    → python3 build_pdf.py scan → merge → pandoc → split → xelatex × 2
    → upload PDF artifact (90天保留)
    → if main branch: create/update Release "book-latest"
```

### 3.2 关键文件
| 文件 | 作用 |
|------|------|
| `.github/workflows/build-book-pdf.yml` | CI 流水线定义 |
| `skills/pdf-toolbook/scripts/build_pdf.py` | 扫描→合并→pandoc→xelatex 全流程 |
| `skills/pdf-toolbook/assets/template/pandoc-template.tex` | LaTeX 模板（ctexbook + 品牌色） |

### 3.3 已修复的 CI 问题（今天修了8个）
1. **timeout 15→90min**：texlive apt 安装太慢
2. **`--no-install-recommends`**：砍掉 texlive 巨型依赖树
3. **`filepath.stem` AttributeError**：str 调了 Path 方法
4. **`mkdir -p output`**：scan 步缺少 output 目录
5. **`--top-level-division=chapter`**：pandoc 默认出 `\section{}` 不是 `\chapter{}`
6. **split 失败 work_dir 未回退**：路径错乱
7. **`-output-directory` 双重嵌套**：`cwd` + `-output-dir` 层叠到错误位置
8. **模板 `BoldFont={Medium}`**：Ubuntu 无此 weight → fontspec nullfont

### 3.4 当前 CI 状态
最后一次推送（`df2de6b`）修复了模板字体问题。**如果 CI 仍然失败，第一件事看 xelatex 输出**——现在的 `run_xelatex` 会把完整错误输出和 .log 打印到 CI log。

---

## 4. 待办

### 优先级 P0
- [ ] **确认 CI 跑通**：等待 `df2de6b` 触发的 run 完成，看 PDF artifact 是否上传
- [ ] **合并 PR #7 → main**：CI 通过后 squash-merge，触发 Release 自动发布
- [ ] **取 PDF**：从 Release `book-latest` 下载 `quadruple-cognition-long-life.pdf`，检查排版

### 优先级 P1
- [ ] **插图生成**：按 `HANDOFF-TO-GPT.md` 指引，用子Agent生成 18 张插图 → `assets/illustrations/`
- [ ] **章节交叉引用检查**：确保章节名/编号一致

### 优先级 P2
- [ ] `docs-checks.yml`（check_style.py / check_links.py）— 现在可能不通过，需逐步修
- [ ] 全书审校

---

## 5. 关键约定

### 5.1 Git 推送方式
**本地 git sandbox 不可靠**，历史经常丢失。推送文件统一用 GitHub REST API：

```python
import base64, json, urllib.request
TOKEN = 'github_pat_...'  # 存在 ~/.workbuddy/MEMORY.md，不要写到仓库里
# GET sha → base64 编码本地文件 → PUT
```

### 5.2 文件名
- 仓库内文件名：**全 ASCII，无空格无中文**（终端友好）
- PDF 输出：`quadruple-cognition-long-life.pdf`
- 插图目录：`assets/illustrations/`（Git LFS 跟踪）

### 5.3 子Agent模式
主Agent写 handoff `.md` 文件 → 告诉用户"花，帮我开个子Agent，Prompt 如下：" → 贴代码块 Prompt → 用户带回结果。

### 5.4 记忆文件
- 项目级：`.workbuddy/memory/YYYY-MM-DD.md`（日常）+ `MEMORY.md`（长期）
- 用户级：`~/.workbuddy/MEMORY.md`（跨项目约定 + PAT）

---

## 6. 常见陷阱

| 陷阱 | 说明 |
|------|------|
| 本地 git 无历史 | 用 GitHub API 推送，别信 `git push` |
| texlive 安装超时 | apt 用 `--no-install-recommends`，timeout ≥ 60min |
| XeLaTeX 静默失败 | `-interaction=nonstopmode` 下 exit 0 ≠ PDF 生成，必须检查 PDF 文件是否存在 |
| CJK 字体名 | Ubuntu `fonts-noto-cjk` 无 "Medium" weight，只用 Regular + Bold |
| Pandoc `# → \section{}` | 不加 `--top-level-division=chapter` 就是 section 不是 chapter |
