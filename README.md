# 4i_e-acc

Workspace initialized on 2026-07-27.

## Layout

- `.git/` — Git repository (initialized via `git init`)
- `.gitignore` — excludes local data, dependencies, secrets, and build output
- `.workbuddy/` — local project memory & state (git-ignored)

## Notes

This repository tracks the project source. Local WorkBuddy memory under
`.workbuddy/` is intentionally ignored so it is never committed.
