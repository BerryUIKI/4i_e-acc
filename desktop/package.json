{
  "name": "quadruple-toolbox",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Quadruple Leverage Toolbox — desktop (Tauri 2 + Rust core, pnpm-managed commands). Reuses ../tools as frontend.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
